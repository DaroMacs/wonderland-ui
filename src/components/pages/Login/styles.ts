import { SxProps, Theme } from "@mui/material/styles";

export const loginContainerStyles: SxProps<Theme> = {
  color: "text.primary",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  padding: 3,
};

export const loginContentStyles: SxProps<Theme> = {
  width: "100%",
  maxWidth: 448,
};
