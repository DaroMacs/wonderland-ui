import { SxProps, Theme } from "@mui/material/styles";

export const badgeContainerStyles: SxProps<Theme> = {
  display: "flex",
  justifyContent: "center",
};

export const megaphoneIconStyles: SxProps<Theme> = {
  fontSize: "12px !important",
};

export const chipStyles: SxProps<Theme> = {
  fontSize: "0.75rem",
  height: "auto",
  py: 0.5,
  px: 1.5,
};
